@model airbnbb.Models.Amenity

<h2>Amenity Details</h2>

<div>
    <h4>@Model.Name</h4>
    <p><strong>Icon:</strong> <img src="@Model.IconUrl" alt="@Model.Name" width="50" height="50" /></p>
</div>

<a asp-action="Index" class="btn btn-secondary">Back to List</a>
